import { app } from '../../../scripts/app.js'

app.registerExtension({
    name: "SpeechDatasetToolkit.uploadAudio",
    async beforeRegisterNodeDef(nodeType, nodeData, app) {
        // TODO: impl
    },
});